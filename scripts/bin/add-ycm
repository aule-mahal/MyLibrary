#!/bin/bash

if [ "$1" == "cpp" ]
then
	cp ~/.ycm_extra_confcpp.py.inert ./.ycm_extra_conf.py
elif [ "$1" == "c" ]
then
	cp ~/.ycm_extra_conf.py.inert ./.ycm_extra_conf.py
else
	echo "Invalid option, try 'c' or 'cpp'."
fi
